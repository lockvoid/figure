import * as React from 'react';

import * as styles from './no_submissions.css!';

export const NoSubmissions = () => (
  <div className={styles.container}>
    <h4 className={styles.h4}>Waiting for submissions</h4>
  </div>
);
